# Exercise

1. Write a small API service that has two connection objects, one master and other replica.

2. Depending upon the request, make call to either master or replica. i.e., reads should be done from replica and writes to master connection.